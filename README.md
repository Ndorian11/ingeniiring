# 🔍 Распознавание объектов с озвучиванием

Система распознавания объектов на изображениях с автоматическим озвучиванием результатов на русском языке.

## 📋 Описание

Этот проект использует YOLOv8 для детекции объектов на изображениях и Google Text-to-Speech (gTTS) для озвучивания результатов. Приложение доступно как через командную строку, так и через веб-интерфейс Streamlit.

## ✨ Возможности

- 🎯 Распознавание до 80 классов объектов
- 🔊 Автоматическое озвучивание результатов на русском языке
- 📊 Визуализация результатов с bounding boxes
- 🌐 Поддержка загрузки изображений из файлов и URL
- ⚙️ Настройка порога уверенности детекции
- 📱 Веб-интерфейс на Streamlit

## 🚀 Установка

### Требования

- Python 3.8 или выше
- pip

### Шаги установки

1. Клонируйте репозиторий:
```bash
git clone <your-repo-url>
cd pythonProject6
```

2. Создайте виртуальное окружение (рекомендуется):
```bash
python -m venv venv
```

3. Активируйте виртуальное окружение:
   - Windows:
   ```bash
   venv\Scripts\activate
   ```
   - Linux/Mac:
   ```bash
   source venv/bin/activate
   ```

4. Установите зависимости:
```bash
pip install -r requirements.txt
```

## 📖 Использование

### Веб-интерфейс (Streamlit)

Запустите Streamlit приложение:
```bash
streamlit run app.py
```

Приложение откроется в браузере по адресу `http://localhost:8501`

**Возможности веб-интерфейса:**
- Загрузка изображений через файл или URL
- Настройка параметров детекции
- Просмотр результатов с визуализацией
- Воспроизведение и скачивание аудио

### Использование как модуль

```python
from object_detection import ObjectDetector

# Инициализация детектора
detector = ObjectDetector('yolov8n.pt')

# Обработка изображения
result = detector.process_image(
    'path/to/image.jpg',
    confidence=0.4,
    language='ru',
    generate_audio=True
)

# Результаты
print(f"Найдено объектов: {result['total_objects']}")
print(f"Текст: {result['text']}")
print(f"Аудио файл: {result['audio_file']}")
```

### Пример скрипта

Создайте файл `example.py`:

```python
from object_detection import ObjectDetector

# Инициализация
detector = ObjectDetector()

# Обработка изображения
result = detector.process_image('demo_image.jpg')

# Вывод результатов
if result:
    print(f"Текст: {result['text']}")
    print(f"Найдено объектов: {result['total_objects']}")
```

Запустите:
```bash
python example.py
```

## 📁 Структура проекта

```
pythonProject6/
├── app.py                 # Streamlit веб-приложение
├── object_detection.py    # Основной модуль распознавания
├── requirements.txt       # Зависимости проекта
├── README.md             # Документация
├── .gitignore           # Игнорируемые файлы
└── output_speech.mp3    # Генерируемый аудио файл (не в git)
```

## ⚙️ Настройки

### Модели YOLO

Доступны следующие модели:
- `yolov8n.pt` - nano (быстрая, меньше точность)
- `yolov8s.pt` - small
- `yolov8m.pt` - medium
- `yolov8l.pt` - large
- `yolov8x.pt` - xlarge (самая точная, медленнее)

Модели автоматически загружаются при первом использовании.

### Параметры

- **confidence** (0.0-1.0): Порог уверенности для детекции объектов
  - Меньше значение = больше объектов, но больше ложных срабатываний
  - Больше значение = меньше объектов, но выше точность

- **language**: Язык озвучивания
  - `'ru'` - русский
  - `'en'` - английский

## 🎯 Поддерживаемые классы объектов

Система распознает 80 классов объектов COCO, включая:
- Люди и животные
- Транспорт (автомобили, велосипеды, самолеты)
- Мебель и бытовая техника
- Еда и напитки
- Спортивный инвентарь
- И многое другое

Полный список классов доступен в `object_detection.py` в словаре `TRANSLATION_DICT`.

## 🔧 Разработка

### Зависимости

Основные библиотеки:
- `ultralytics` - YOLOv8
- `gtts` - Google Text-to-Speech
- `streamlit` - Веб-интерфейс
- `torch` - PyTorch
- `opencv-python` - Обработка изображений
- `Pillow` - Работа с изображениями

### Тестирование

Для тестирования используйте тестовые изображения:
```python
from object_detection import ObjectDetector

detector = ObjectDetector()
result = detector.process_image('https://ultralytics.com/images/bus.jpg')
```

## 📝 Лицензия

Этот проект создан в образовательных целях.

## 🤝 Вклад

Приветствуются любые улучшения! Создавайте issues и pull requests.

## 📧 Контакты

Для вопросов и предложений создавайте issues в репозитории.

---

**Примечание**: При первом запуске модель YOLO будет автоматически загружена из интернета. Убедитесь, что у вас есть подключение к интернету.

