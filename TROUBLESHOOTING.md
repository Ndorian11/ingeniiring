# 🔧 Решение проблем

## Ошибка: `libGL.so.1: cannot open shared object file`

### Проблема
Эта ошибка возникает при использовании `opencv-python` в серверных окружениях (Linux без GUI, Docker контейнеры, Streamlit Cloud и т.д.).

### Решение

Проект уже настроен на использование `opencv-python-headless`, который не требует системных графических библиотек.

**Если ошибка все еще возникает:**

1. Удалите старую версию OpenCV:
```bash
pip uninstall opencv-python opencv-contrib-python
```

2. Установите headless версию:
```bash
pip install opencv-python-headless
```

3. Или переустановите все зависимости:
```bash
pip install -r requirements.txt --force-reinstall
```

### Альтернативное решение (для системных администраторов)

Если вы хотите использовать обычный `opencv-python`, установите системные библиотеки:

**Ubuntu/Debian:**
```bash
sudo apt-get update
sudo apt-get install -y libgl1-mesa-glx libglib2.0-0
```

**CentOS/RHEL:**
```bash
sudo yum install -y mesa-libGL mesa-libGL-devel
```

**Однако рекомендуется использовать `opencv-python-headless` для серверных приложений.**

## Другие распространенные проблемы

### Ошибка загрузки модели YOLO
**Симптомы:** `FileNotFoundError` или ошибки загрузки модели

**Решение:**
- Убедитесь, что есть подключение к интернету
- Модель автоматически загружается при первом использовании
- Проверьте доступность https://github.com/ultralytics/assets

### Ошибка синтеза речи (gTTS)
**Симптомы:** `gtts.tts.gTTSError` или таймауты

**Решение:**
- Требуется подключение к интернету для работы gTTS
- Проверьте доступность Google TTS сервисов
- При проблемах с сетью можно временно отключить генерацию аудио

### Медленная работа приложения
**Решение:**
- Используйте модель `yolov8n.pt` (nano) вместо более крупных моделей
- Уменьшите разрешение входных изображений
- Увеличьте порог уверенности (confidence) для фильтрации объектов

### Проблемы с памятью
**Симптомы:** `OutOfMemoryError` или зависания

**Решение:**
- Используйте более легкую модель (`yolov8n.pt`)
- Обрабатывайте изображения меньшего размера
- Закрывайте неиспользуемые приложения

### Проблемы с зависимостями
**Симптомы:** Конфликты версий библиотек

**Решение:**
```bash
# Создайте чистое виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установите зависимости
pip install -r requirements.txt
```

## Получение помощи

Если проблема не решена:
1. Проверьте версии Python (требуется 3.8+)
2. Убедитесь, что все зависимости установлены
3. Проверьте логи ошибок для деталей
4. Создайте issue в репозитории с описанием проблемы

